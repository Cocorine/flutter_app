import 'package:flutter/material.dart';
import 'package:mobile_app/models/category.dart';
import 'package:mobile_app/api/categories_api.dart';
import 'package:mobile_app/screens/category_posts.dart';
import 'package:mobile_app/screens/categories_list.dart';

class MainDrawer extends StatefulWidget {

  @override
  _MainDrawerState createState() => _MainDrawerState();
}

class _MainDrawerState extends State<MainDrawer> {

  @override
  Widget build(BuildContext context) {
    // This method is rerun every time setState is called, for instance as done
    // by the _incrementCounter method above.
    //
    // The Flutter framework has been optimized to make rerunning build methods
    // fast, so that you can just rebuild anything that needs updating rather
    // than having to individually change instances of widgets.
    return Container();
  }
}